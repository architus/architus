apiVersion: apps/v1
kind: Deployment
metadata:
  name: logs-gateway-ingress
  labels:
    app: logs-gateway-ingress
spec:
  selector:
    matchLabels:
      app: logs-gateway-ingress
  template:
    metadata:
      labels:
        app: logs-gateway-ingress
    spec:
      containers:
        - name: logs-gateway-ingress
          image: "{{prefix}}logs-gateway-ingress:{{tag}}"
          args: ["/etc/architus/config.d/service.toml"]
          env:
            - name: LOGS_GATEWAY_INGRESS_CONFIG_SECRETS__DISCORD_TOKEN
              valueFrom:
                secretKeyRef:
                  name: architus-secret
                  key: bot_token
          volumeMounts:
            - name: config
              mountPath: "/etc/architus/config.d"
              readOnly: true
      imagePullSecrets:
        - name: regcred
      volumes:
        - name: config
          configMap:
            name: logs-gateway-ingress-config
